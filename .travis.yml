language: java
jdk:
  - openjdk8

branches:
  only:
    - master


# Travis CI(지속적인 통합) Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"


# CI 실행 완료 시 메일로 알람

notifications:
  email:
    recipients:
      - a79007714@gmail.com
